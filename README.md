# ThinkPad T490s Windows 11 Installation Guide (Apex-Deployment Protocol)

This repository contains the definitive, bare-metal deployment protocol for installing Windows 11 on the Lenovo ThinkPad T490s (10th Gen Intel). It bypasses common issues such as:
- **Error 0x8007000D**: Caused by the 4GB FAT32 file size limit on large ISOs.
- **Invisible SSD**: Caused by missing Intel RST (VMD/RAID) drivers in the standard Windows installer.
- **Boot Loops**: Caused by incorrect partition tables or USB priority.

## 🚀 The Multi-Layered Solution

### 1. The USB Architecture (Dual-Partition)
To defeat the 4GB barrier, the USB must be partitioned as follows:
- **UEFI_BOOT (1GB, FAT32)**: Contains the UEFI entry points.
- **WIN11_DATA (Remaining, NTFS)**: Contains the full, uncorrupted `install.wim` (7.5GB+).

### 2. The Apex-Deployment Script (Manual Deployment)
When the Windows Setup GUI fails to show your drive, use the included `APEX_DEPLOY_V3.bat`. It bypasses the Wizard and uses the **DISM Engine** to:
1.  Search all drives for the Windows Image.
2.  Partition the internal SSD (Disk 0) as GPT.
3.  Apply the image directly to the drive.
4.  **Inject Official Lenovo V18.7 Drivers** into the offline OS.
5.  Configure the UEFI Bootloader.

## 📂 Repository Contents
- `/web_guide`: A high-fidelity HTML5/CSS3 interactive guide.
- `/latex_source`: Technical manual source for professional PDF generation.
- `/rust_tool`: Source code for a conceptual high-performance Rust deployment utility.

## 🛠️ Requirements
- Verified Windows 11 ISO (English International x64).
- Intel RST v18.7 Drivers (Extracted).
- A Saturday you don't want to waste.

---
*Generated by Antigravity OS AI Cluster (v6.5)*
